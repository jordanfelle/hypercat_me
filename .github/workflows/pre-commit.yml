name: Pre-commit checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  pre-commit:
    # Only auto-fix on push events; PRs from forks can't push anyway
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref || github.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Cache pre-commit environments
        uses: actions/cache@v5
        with:
          path: ~/.cache/pre-commit
          key: ${{ runner.os }}-pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pre-commit-

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Set up Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "0.154.5"
          extended: true

      - name: Cache Hugo resources
        uses: actions/cache@v5
        with:
          path: content/resources/_gen
          key: >-
            ${{ runner.os }}-hugo-resources-${{
            hashFiles('content/**', 'content/assets/**',
            'content/hugo.yaml') }}
          restore-keys: |
            ${{ runner.os }}-hugo-resources-

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Run pre-commit checks
        id: pre-commit
        continue-on-error: true
        run: pre-commit run --all-files

      - name: Show diff if pre-commit made changes
        if: steps.pre-commit.outcome == 'failure'
        run: |
          if git diff --quiet && [ -z "$(git status --porcelain)" ]; then
            echo "Pre-commit checks failed, but no file changes (including untracked files) were detected."
          else
            echo "Pre-commit hooks made the following changes (including any new/untracked files):"
            git diff HEAD || true
            echo
            echo "Git status (including untracked files):"
            git status --porcelain || true
          fi

      - name: Commit and push fixes (PR only)
        if: steps.pre-commit.outcome == 'failure' && github.event_name == 'pull_request'
        run: |
          if ! git diff --quiet || [ -n "$(git status --porcelain)" ]; then
            last_author=$(git log -1 --pretty=%an)
            last_subject=$(git log -1 --pretty=%s)
            if [ "$last_author" = "github-actions[bot]" ] && echo "$last_subject" | grep -qi "pre-commit fixes"; then
              echo "Pre-commit fixes already applied by the bot; refusing to loop."
              exit 0
            fi
            git add -A
            git commit -m "chore: apply pre-commit fixes"
            git push
            echo "✅ Pre-commit fixes committed and pushed to PR"
          else
            echo "❌ Pre-commit checks failed but no fixes were applied"
            exit 1
          fi

      - name: Commit and push fixes (main branch)
        if: steps.pre-commit.outcome == 'failure' && github.event_name == 'push'
        run: |
          if ! git diff --quiet || [ -n "$(git status --porcelain)" ]; then
            last_author=$(git log -1 --pretty=%an)
            last_subject=$(git log -1 --pretty=%s)
            if [ "$last_author" = "github-actions[bot]" ] && echo "$last_subject" | grep -qi "pre-commit fixes"; then
              echo "Pre-commit fixes already applied by the bot; refusing to loop."
              exit 0
            fi
            git add -A
            git commit -m "chore: apply pre-commit fixes"
            git push
            echo "✅ Pre-commit fixes committed and pushed to main"
          else
            echo "❌ Pre-commit checks failed but no fixes were applied"
            exit 1
          fi

      - name: Build Hugo site
        if: >-
          steps.pre-commit.outcome != 'failure' ||
          (steps.pre-commit.outcome == 'failure' && github.event_name == 'push')
        run: |
          echo "Building Hugo site..."
          cd content
          npm install
          npm run build
          if [ ! -d "public" ] || [ ! -f "public/index.html" ]; then
            echo "Error: Hugo build failed - public/index.html not found"
            exit 1
          fi
          cd ..
          echo "Build successful"

      - name: Validate photo links
        if: >-
          steps.pre-commit.outcome != 'failure' ||
          (steps.pre-commit.outcome == 'failure' && github.event_name == 'push')
        run: bash scripts/validate-photo-links.sh

      - name: Validate HTML structure
        if: steps.pre-commit.outcome != 'failure'
        run: |
          if ! command -v tidy &> /dev/null; then
            sudo apt-get update && sudo apt-get install -y tidy
          fi
          find content/public -name '*.html' -type f | head -20 | while read -r file; do
            tidy -q -e "$file" 2>&1 | head -5
          done
          echo "HTML validation completed"

      - name: Validate project configuration
        if: steps.pre-commit.outcome != 'failure'
        run: bash scripts/validate-config.sh

      - name: Validate internal links
        if: steps.pre-commit.outcome != 'failure'
        run: bash scripts/validate-links.sh

      - name: Run BATS tests
        if: steps.pre-commit.outcome != 'failure'
        run: |
          npm install -g bats
          bats tests/validate-photo-links.bats
